<div >
  <h1>¡Hola, {{user.name}} {{user.Last_name}}! ¡Que gusto verte!</h1>
  <p>tu edad es {{user.edad}}</p>
  <p>tu correo es {{user.correo}}</p>
  <p>tu telefono es {{user.telefono}}</p>

  {{#if isAdmin}}
    {{#each food}}
      <div>
        <p>{{this.name}}</p>
        <p>{{this.price}}</p>
      </div>
    {{/each}}
  {{else}}
    <p>No tienes permisos para ver la lista de alimentos</p>
  {{/if}}
</div>
<script src="js/index.js"></script>